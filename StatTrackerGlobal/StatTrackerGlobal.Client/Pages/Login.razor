@page "/"

@inherits Fluxor.Blazor.Web.Components.FluxorComponent

@using Fluxor
@using StatTrackerGlobal.Client.Pages
@using StatTrackerGlobal.Client.Store
@using StatTrackerGlobal.Shared

<h3>Select a valid json</h3>


<button class="btn btn-primary" @onclick="JustGoToAPlace">Just go to a place</button>


@code {
    //Inputfiles was kind of a stupid idea for a web app, even if it's designed to be used by one team one seaon at a time
        //Try going back to data stored on the server.
    public class LoginViewModel
    {
        public string Username { get; init; }
        public string Password { get; init; }
        public string TeamName { get; init; }
    }
    public LoginViewModel ViewModel = new LoginViewModel();

    private void SelectFilePath(InputFileChangeEventArgs e)
    {
        SelectedFilePath = e.File.Name;
    }
}
